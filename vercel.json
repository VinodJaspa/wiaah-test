{
  "installCommand": "corepack prepare pnpm@10.17.0 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm build",
  "build": {
    "env": {
      "NODE_VERSION": "20.11.1",
      "PNPM_HOME": "/vercel/.pnpm"
    }
  }
}
