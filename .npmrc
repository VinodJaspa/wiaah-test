# Always use the lockfile for reproducibility
frozen-lockfile=true

# Ensure installs fail fast if lockfile is out of sync
prefer-frozen-lockfile=true

# Auto-create lockfile if missing locally
lockfile-only=false

# Ensure consistent package manager behavior across environments
strict-peer-dependencies=false

# Always save exact versions in overrides
save-exact=true
