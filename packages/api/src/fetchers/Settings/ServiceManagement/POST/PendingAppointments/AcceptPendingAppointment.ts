import { AcceptPedningAppointmentDto } from "dto";
import { FetchingMock } from "utils";

export const AcceptPendingAppointment = (
  input: AcceptPedningAppointmentDto
) => {
  return FetchingMock;
};
