import React from "react";
import { IconBaseProps } from "react-icons";
import { HiViewGrid } from "react-icons/hi";

export const OpenFilterIcon: React.FC<IconBaseProps> = (props) => {
  return <HiViewGrid {...props} />;
};
