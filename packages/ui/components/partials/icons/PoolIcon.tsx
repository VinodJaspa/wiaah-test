import React from "react";
import { IconBaseProps } from "react-icons";
import { GiPoolDive } from "react-icons/gi";
export const PoolIcon: React.FC<IconBaseProps> = (props) => {
  return <GiPoolDive {...props} />;
};
