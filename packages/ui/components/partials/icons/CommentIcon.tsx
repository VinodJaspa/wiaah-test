import React from "react";
import { IconBaseProps } from "react-icons";
import { BiComment } from "react-icons/bi";

export const CommentIcon: React.FC<IconBaseProps> = (props) => {
  return <BiComment {...props} />;
};
