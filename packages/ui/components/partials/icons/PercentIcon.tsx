import React from "react";
import { IconBaseProps } from "react-icons";
import { BsPercent } from "react-icons/bs";

export const PercentIcon: React.FC<IconBaseProps> = (props) => {
  return <BsPercent {...props} />;
};
