import React from "react";
import { IconBaseProps } from "react-icons";
import { BiWindow } from "react-icons/bi";

export const CarWindowIcon: React.FC<IconBaseProps> = (props) => {
  return <BiWindow {...props} />;
};
