import { storybookSectionsTitle, PrivacySection } from "@UI";
import { Meta } from "@storybook/react";

export default {
  title: "UI / sections / PrivacySection",
} as Meta<typeof PrivacySection>;

export const Default = () => <PrivacySection />;
