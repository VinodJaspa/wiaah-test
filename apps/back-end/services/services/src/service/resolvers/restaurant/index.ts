import { RestaurantDishResolver } from './dish.resolver';
import { RestaurantMenuResolver } from './menu.resolver';

export const restaurantResolvers = [
  RestaurantDishResolver,
  RestaurantMenuResolver,
];
