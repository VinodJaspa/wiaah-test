import { ProfileStatistics } from "@UI";
import { NextPage } from "next";
import React from "react";

const Preview: NextPage = () => {
  return <ProfileStatistics accountId="" />;
};

export default Preview;
